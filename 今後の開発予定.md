# 🚀 楽天FX Goクオンツ パイプライン - 開発実績 ＆ ロードマップ

本ドキュメントは、**「マルチフィルタ型逆張りアルゴリズム（BB + RSI + MTF-ATR + ADX）」** および **「AI共創・動的レジーム適応型生存戦略」** を完全実装した楽天MT4 AIクオンツパイプラインのアーキテクチャおよび開発ロードマップです。

---

## 🌟 AIトレード時代における唯一の生存戦略哲学（Core Philosophy）

> **「FXはゼロサムゲームであり、AIの普及によって市場の効率性は高まり、手法の陳腐化スピードはさらに加速する。**  
> **そのため、一度構築したシステムに依存するのではなく、常に新しい市場の癖や傾向をAIと共に分析し、ロジックをアップデートし続ける姿勢が求められる。**  
> **かつて電話注文に固執した人々がオンライン取引に淘汰されたように、AIという強力な武器を拒絶することは、現代のトレードにおいて致命的な不利を招く。**  
> **AIを『楽をして稼ぐ道具』ではなく、『戦略の具体化と実行を最適化する不可欠なパートナー』として定義し直すこと。これこそが、AIトレード時代における唯一の生存戦略である。」**

---

## 🏆 これまでの完了実績（Current Achievements）

- [x] **Rust 超高速 Gateway ＆ マルチフィルタ戦略エンジン (`crates/gateway`)**:
  - `BollingerBands(20, 2.0σ)` と `RSI(14)` の条件付き確率 AND 判定によるバンドウォーク回避
  - `MTF-ATR(14, 1.5倍フィルター)` および `ADX(14, >=25フィルター)` による異常ボラティリティ・強トレンド遮断
  - **4ステート相場レジーム判定**（`Purple`, `Orange`, `Red`, `Clear`）
  - **59分台次時間先読みロジック**（次時間停止なら59分での新規エントリー拒否）
  - **ドテン（Reverse）全決済・反転発注 ＆ ピラミッティング（最大2ポジション）**
- [x] **Go バックエンド ＆ SQLite データ基盤 (`cmd/server`)**:
  - Gin フレームワークによる超高速 REST API 移植（メモリ消費量 ~25MB）
  - `modernc.org/sqlite` (CGOレス) による軽量データベースエンベディング (WALモード)
  - `web.StaticFS` (`//go:embed`) によるフロントエンド（HTML/CSS/JS/Chart.js）の完全単一バイナリ統合
  - `/api/regime` エンドポイントによる 4ステート相場コンテキスト配信
- [x] **Go AI Kill-Switch IPC サーバー (`internal/infrastructure/ipc`)**:
  - TCP Port 5556 で Rust Gateway からのシグナル検証・キルスイッチ判定をマイクロ秒処理
- [x] **Gemini API Direct Go Client (`internal/infrastructure/ai`)**:
  - Pythonプロセス完全撤廃、Goから直接Gemini 2.5 Flashで 4ステート相場環境解説および **プロフィットファクター (PF 1.30+) 目標達成診断** を自動生成
- [x] **WebSocket リアルタイム Hub (`internal/handler/http/ws.go`)**:
  - 損益・シグナル・相場レジーム・キルスイッチ状態の双方向リアルタイムPUSH通知
- [x] **Pro Trader HUD Web コクピット (`web/static`)**:
  - 4ステート発光バッジ（PURPLE, ORANGE, RED, CLEAR）
  - BB+RSI 平均回帰シグナルリスト ＆ Chart.js 累積損益曲線
- [x] **プロ仕様 MT4 チャートGUI ＆ HUD コクピット (`mql4/RakutenTradeAgent.mq4`)**:
  - 4ステートHUD ＆ リアルタイムPnLメーター描画
  - 24時間別稼働制御（0〜23時フラグ）＆ 59分台次時間先読みロジック
  - タイムベース強制決済（120分タイムアウト自動クローズ）
  - ピラミッティング（最大2）＆ 土転（Reverse）自動執行
- [x] **一括起動スクリプト統合 (`scripts/start_all.bat` / `scripts/stop_all.bat`)**:
  - Go 単一バイナリ + Rust Gateway の多重起動防止付き一括起動・停止管理

---

## 📅 今後の開発予定（Next Phase Roadmap）

### 🌟 Phase 1: 通貨ペア別動的閾値チューニング ＆ 最適化 (PF 1.30+ 安定化)

1. **通貨ペア別動的 ADX / ATR 閾値プリセット**:
   - USDJPY: ADX 25 / GBPJPY: ADX 15〜18 / XAUUSD: ADX 30 などの自動スキャリング。
2. **FE (Fibonacci Expansion) 161.8% / 261.8% ターゲット算出計算器**:
   - Go の高速浮動小数点演算により、リアルタイムで利確目標数値を全クライアントへ配信。

---

### 🌟 Phase 2: Web UI ＆ スマホ通知の高度化

1. **マルチモーダルチャート反省会サムネイルビュー**:
   - エントリー時チャート画像を SQLite / ローカルストレージに保持し、Web UI 上でサムネイル比較表示。
2. **Web Push API (Go 側バックエンド通知エンジン)**:
   - Go の `golang.org/x/crypto` 等を活用した Web Push エンジンにより、相場急変・キルスイッチ発動時にスマホへ即座に通知。

---

### 🌟 Phase 3: データ基盤・バックテスト・マルチスレッド最適化

1. **月別 SQLite 自動ローテーション ＆ WAL モードチューニング**:
   - `trade_pipeline_YYYYMM.db` への月次自動アーカイビング機能を Go のバックグラウンドワーカーとして実装。
2. **Go ゴルーチン（Goroutines）を活用した超高速マルチスレッド・モンテカルロシミュレーション**:
   - 過去10年分のヒストリカルデータに対し、並列計算でマルチフィルタ逆張り（BB/RSI/ATR/ADX）の最適パラメータを数秒で算出。

---

## 📌 運用・実行コマンド一覧

- **Go 単一バイナリ ビルド**:
  `go build -ldflags="-s -w" -o rakutenfx.exe ./cmd/server`
- **Rust Gateway ビルド**:
  `cargo build --bin gateway`
- **Go 開発用サーバー起動**:
  `go run ./cmd/server/main.go`
- **全単体テスト実行**:
  `cargo test -p gateway ; go test -v ./internal/usecase/...`
- **一括起動（Rust Gateway + Go Server + ブラウザUI）**:
  `scripts\start_all.bat` を実行