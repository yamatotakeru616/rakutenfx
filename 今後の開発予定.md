# 🚀 楽天FX AIクオンツ パイプライン - 今後の開発予定ロードマップ

本ドキュメントは、**「上位足フィボナッチ × 下位足ダウ理論極小損切り（4.0pips）手法」** を完全実装した楽天MT4 AIクオンツパイプラインの今後の開発・拡張予定ロードマップをまとめたものです。

---

## 🏆 これまでの完了実績（Current Achievements）

- [x] **Rust 超高速 Gateway サーバー (`crates/gateway`)**:
  - TCPソケット（Port 5555）によるマイクロ秒レスポンス
  - M1バー生成、ATR・RSI・上位足フィボナッチ・下位足ダウ転換シグナルエンジン
  - ゼロアロケーション指向バッファ再利用による省メモリ設計
- [x] **Python AI キルスイッチ ＆ DuckDB OLAP エンジン (`python/evaluator`)**:
  - RTX 3050 Ti GPU を活用したチャート画像評価 ＆ ミリ秒パターン照合
  - リアルタイム相場レジーム検知（`STRONG_TREND_BULL` / `HIGH_VOLATILITY_EXPANSION`）
  - 週次自律パラメータチューナー（Gemini 3.6 連携による `config.toml` 自動更新）
- [x] **プロ仕様 MT4 チャートGUI ＆ 操作コクピット (`mql4/RakutenTradeAgent.mq4`)**:
  - 1時間足（H1）スイング高安値からのマルチタイムフレーム（MTF）自動フィボナッチ描画
  - 50.0%〜61.8% 黄金比反発ゾーンの半透明ハイライトボックス（`OBJ_RECTANGLE`）
  - 黄金ゾーン侵入時の音声チャイム通知（`PlaySound("alert.wav")`）
  - 半透明ダーク背景パネル付きリアルタイムHUD（レジーム、AIキルスイッチ、リアルタイム獲得pips・含み損益）
  - 直近3件の確定損益＆合計損益メーター表示
  - 右上4大操作ボタン（`[!] EMERGENCY CLOSE ALL`, `[LOCK] BE LOCK`, `[LOT: 0.10/0.25/0.50L]`, `[+] BUY / [-] SELL`）
  - +10.0pips利益からの自動トレーリングストップ（Trailing SL）
- [x] **Web ダッシュボード ＆ GitHub Pages 自動公開**:
  - [https://yamatotakeru616.github.io/rakutenfx/](https://yamatotakeru616.github.io/rakutenfx/) にて常時閲覧可能
  - スマホ用 PWA（ホーム画面アイコン登録、スタンドアローン起動）
  - LIVEパルスインジケーター、ダーク/サイバーテーマ切替、60秒自動更新
- [x] **CI/CD ＆ Windows 自動起動パイプライン**:
  - GitHub Actions CI（`cargo test` 全6件合格、`pytest` 単体テスト自動実行）
  - 週次一括実行ランナー（`scripts/run_tuner.bat`）
  - Windows タスクスケジューラ自動起動スクリプト（`scripts/register_autostart_task.ps1`）

---

## 📅 今後の開発予定（Next Phase Roadmap）

### 🌟 Phase 1: MT4 操作性 ＆ 資金管理の高度化

1. **連勝/連敗時の自動ロット推奨ハイライト機能**:
   - 直近の取引履歴（連勝/連敗ペース）を自動診断し、確信度が高い局面ではロットボタンをグリーン（推奨0.50Lot）、警戒局面ではイエロー（0.10Lot）に視覚サジェスト。
2. **フィボナッチ目標エクスパンション（FE 161.8%）の自動利確ターゲット描画**:
   - 上位足スイングから自動算出した FE 161.8% / 261.8% の大目標ラインを点線でチャート上にプロット。
3. **分割利確（Scale-Out）自動化機能**:
   - +15.0pips到達時に保有ポジションの50%を部分利確し、残り50%のSLを建値プラスへ自動移動するハイブリッド利確。

---

### 🌟 Phase 2: Web ダッシュボード（GitHub Pages）のビジュアル強化

1. **直近1週間の勝率・損益比円グラフの追加**:
   - Webダッシュボード上に、ロング/ショート別勝率、時間帯別勝率の円グラフ・棒グラフを追加。
2. **実取引スクリーンショットのギャラリー表示機能**:
   - エントリー時のMT4チャート画像（M1ダウ転換ブレイクの瞬間）をWeb上で拡大プレビュー可能にする。
3. **PWA プッシュ通知（Web Push API）**:
   - スマホのPWAアプリアイコン宛てに、週次Geminiチューニング完了通知を配信。

---

### 🌟 Phase 3: バックテスト・データ基盤の堅牢化

1. **月別 SQLite データベース自動アーカイブ機能**:
   - 1ヶ月ごとに `trade_pipeline.db` を月別ファイル（例: `trade_pipeline_202608.db`）へ自動ローテーションし、ミリ秒検索性能を永続維持。
2. **DuckDB による過去10年分ヒストリカルデータの一括OLAPシミュレーション**:
   - 楽天証券USD/JPYおよびGOLDのティックデータをDuckDBに取り込み、極小損切り（4.0pips）の長期モンテカルロ検証を実施。
3. **Gemini 3.6 マルチモーダルによる週次チャート反省会レポートの自動生成**:
   - 毎週日曜日、負けトレードのチャート画像トップ3をGeminiが視覚分析し、「改善ポイント」を自動執筆する週報機能。

---

## 📌 運用・実行コマンド一覧

- **一括起動（Rust Gateway + Python IPC）**:
  `scripts\start_all.bat` をダブルクリック
- **週次パラメータ最適化 ＆ ダッシュボード更新**:
  `scripts\run_tuner.bat` をダブルクリック
- **Windows 自動起動タスク登録**:
  PowerShell（管理者）で `scripts\register_autostart_task.ps1` を実行
- **Web ダッシュボード URL**:
  [https://yamatotakeru616.github.io/rakutenfx/](https://yamatotakeru616.github.io/rakutenfx/)
