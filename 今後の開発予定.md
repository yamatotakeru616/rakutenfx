# 🚀 楽天FX Goクオンツ パイプライン - 今後の開発予定ロードマップ

本ドキュメントは、**「上位足フィボナッチ × 下位足ダウ理論極小損切り（4.0pips）手法」** を完全実装した楽天MT4 AIクオンツパイプラインの Go言語（Gin + SQLite + go:embed）移行後の開発ロードマップです。

---

## 🏆 これまでの完了実績（Current Achievements）

- [x] **Rust 超高速 Gateway サーバー (`crates/gateway`)**: TCP Port 5555 によるマイクロ秒レスポンスとM1バー・フィボナッチ生成エンジン
- [x] **Go バックエンド ＆ SQLite データ基盤 (`cmd/server`)**:
  - Gin フレームワークによる高速 REST API 移植（メモリ消費量 ~25MB）
  - `modernc.org/sqlite` (CGOレス) による軽量データベースエンベディング (WALモード)
  - `web.StaticFS` (`//go:embed`) によるフロントエンド（HTML/CSS/JS/Chart.js）の完全単一バイナリ統合
- [x] **Go AI Kill-Switch IPC サーバー (`internal/infrastructure/ipc`)**: TCP Port 5556 で Rust Gateway からのシグナル検証・キルスイッチ判定をマイクロ秒処理
- [x] **Gemini API Direct Go Client (`internal/infrastructure/ai`)**: Pythonプロセス完全撤廃、Goから直接Gemini 2.5 Flashで週次反省会・トレード評価レポートを自動生成
- [x] **WebSocket リアルタイム Hub (`internal/handler/http/ws.go`)**: 損益・シグナル・キルスイッチ状態の双方向リアルタイムPUSH通知
- [x] **Pro Trader HUD Web コクピット (`web/static`)**: Cyberpunk/ダークテーマの洗練されたSPA（Chart.js 損益曲線・勝率/PFメーター・緊急キルスイッチ）
- [x] **プロ仕様 MT4 チャートGUI ＆ HUD コクピット (`mql4/RakutenTradeAgent.mq4`)**: 自動フィボナッチ描画・アラート機能・緊急全決済ボタン統合
- [x] **一括起動スクリプト統合 (`scripts/start_all.bat` / `scripts/stop_all.bat`)**: Go 単一バイナリ + Rust Gateway の安全な一括起動・停止管理

---

## 📅 今後の開発予定（Next Phase Roadmap）

### 🌟 Phase 1: クオンツ演算・リアルタイムシグナルエンジンの拡張

1. **FE (Fibonacci Expansion) 161.8% / 261.8% ターゲット算出計算器**:
   - Go の高速浮動小数点演算により、リアルタイムで利確目標数値を全クライアントへ配信。
2. **直近ボラティリティ（ATR）に基づく動的SL/TP微調整**:
   - スプレッド拡大や経済指標前の自動フィルター強化。

---

### 🌟 Phase 2: Web UI ＆ スマホ通知の高度化

1. **マルチモーダルチャート反省会サムネイルビュー**:
   - エントリー時チャート画像を SQLite / ローカルストレージに保持し、Web UI 上でサムネイル比較表示。
2. **Web Push API (Go 側バックエンド通知エンジン)**:
   - Go の `golang.org/x/crypto` 等を活用した Web Push エンジンにより、相場急変・キルスイッチ発動時にスマホへ即座に通知。

---

### 🌟 Phase 3: データ基盤・バックテスト・マルチスレッド最適化

1. **月別 SQLite 自動ローテーション ＆ WAL モードチューニング**:
   - `trade_pipeline_YYYYMM.db` への月次自動アーカイビング機能を Go のバックグラウンドワーカーとして実装。
2. **Go ゴルーチン（Goroutines）を活用した超高速マルチスレッド・モンテカルロシミュレーション**:
   - 過去10年分のヒストリカルデータに対し、並列計算で極小損切り（4.0pips）の最適パラメータを数秒で算出。

---

## 📌 運用・実行コマンド一覧

- **Go 単一バイナリ ビルド**:
  `go build -ldflags="-s -w" -o rakutenfx.exe ./cmd/server`
- **Go 開発用サーバー起動**:
  `go run ./cmd/server/main.go`
- **Go 単体テスト実行**:
  `go test -v ./internal/usecase/...`
- **一括起動（Rust Gateway + Go Server）**:
  `scripts\start_all.bat` を実行